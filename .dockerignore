.git/
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Datasets and reports are regenerated; artifacts are mounted or built by the
# training job rather than baked into the serving image.
data/
reports/
artifacts/

.DS_Store
*.md
!README.md

# Deployment and tooling config is not needed inside the image. `docker/` is
# deliberately absent from this list — the build copies the entrypoint from it.
k8s/
.github/
.devcontainer/

