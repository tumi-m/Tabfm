apiVersion: v1
kind: Service
metadata:
  name: tabfm-lab-api
  namespace: tabfm-lab
  labels:
    app.kubernetes.io/name: tabfm-lab
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tabfm-lab
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
